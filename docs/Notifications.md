# Notifications

## Email notifications
Email notifications are configured per user in Settings → Notifications.
You can enable or disable emails for invoices, mentions and weekly summaries.

## Webhooks
Webhooks let external systems receive events (invoice.paid, user.created).
Configure endpoint URLs in Settings → Webhooks; each event is signed with a secret.
